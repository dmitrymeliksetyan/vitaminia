// Этап "Выделение AI Worker в отдельный независимый сервис".
//
// СЛЕЗАНО: этот файл раньше был единой точкой конфигурации ВСЕХ денежных/
// временных порогов AI-конвейера (SLA-таргеты, heartbeat, backoff,
// concurrency и т.п.) — вся эта конфигурация нужна ТОЛЬКО коду, который
// реально исполняет этапы/AI-вызовы, а он теперь целиком в medizin-worker
// (см. medizin-worker/src/config/production-config.ts — полная версия с тем
// же смыслом констант). SSR оставляет себе ТОЛЬКО то, что реально
// используется здесь: шаг увеличения бюджета для кнопки «Продолжить ещё на
// $0.50» (extend-budget.ts) — единственный AI-конвейерный параметр, которым
// в принципе управляет человек через SSR-роут, а не сам Worker.

/** Шаг для кнопки «Продолжить ещё на $0.50» — на СКОЛЬКО поднимаем лимит конкретного job при явном подтверждении администратора. */
export const BUDGET_EXTENSION_STEP_USD = 0.5;
