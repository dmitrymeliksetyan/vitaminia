---
// Этап 7 ТЗ (Часть 10, обязательное изменение) — production статьи имеет
// СОБСТВЕННЫЙ реальный URL, отдаётся сервером (не клиентским pushState).
// Обновление страницы и прямая ссылка сохраняют открытую статью нативно.
export const prerender = false;
import AppLayout from "../../../../layouts/AppLayout.astro";
import EditorialApp from "../../../../components/admin/EditorialApp";

const { id } = Astro.params;
---
<AppLayout title="Производство — Vitaminia">
  <EditorialApp client:load section="jobs" jobId={id} />
</AppLayout>
